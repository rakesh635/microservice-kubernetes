apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  labels:
    app: hystrix-dashboard
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: hystrix-dashboard
