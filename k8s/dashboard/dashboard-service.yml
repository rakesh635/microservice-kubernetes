apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  labels:
    app: hystrix-dashboard
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  type: NodePort
  selector:
    app: hystrix-dashboard
