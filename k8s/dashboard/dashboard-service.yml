apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  labels:
    app: hystrix-dashboard
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
      nodePort: 30005
  selector:
    app: hystrix-dashboard
