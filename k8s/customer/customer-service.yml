apiVersion: v1
kind: Service
metadata:
  name: customer-service
  labels:
    app: customer
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  type: NodePort
  selector:
    app: customer
