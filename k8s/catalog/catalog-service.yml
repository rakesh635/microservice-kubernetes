apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  labels:
    app: catalog
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
      nodePort: 33331
  selector:
    app: catalog
