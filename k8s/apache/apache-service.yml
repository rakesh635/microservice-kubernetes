apiVersion: v1
kind: Service
metadata:
  name: apache-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: 0.0.0.0/0
  labels:
    app: apache
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: apache
  type: NodePort
