apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  type: NodePort
  selector:
    app: order
